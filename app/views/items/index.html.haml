.section.item_search
  .container
    .row
      - if @items.size > 0
        %table.table.table-striped.table-condensed
          %tr
            %th Title
            %th Manufacturer
          - @items.each do |item|
            %tr
              %td= image_tag item.image_url, class: 'img-thumbnail'
              %td
                %h4
                  = link_to item.title, new_post_url(asin: item.asin)
                  %small= "[#{item.item_binding}]"
                = item.author
                %br
                = item.manufacturer
                %small= "(#{item.release_date})"
 
        = paginate @items
      - else
        検索結果が見つかりません
